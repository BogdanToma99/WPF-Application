<UserControl x:Class="WordAddIn1.Controls.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WordAddIn1.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    
    <Grid Width="430">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel
			Orientation="Horizontal"
			Grid.Column="1" 
			HorizontalAlignment="Right">
            <Image 
				Width="18"
				Height="18"
				VerticalAlignment="Center"
				Margin="0, 0, 20, 0"
				Source="../Resources/ClearColored.png">
                <Image.InputBindings>
                    <MouseBinding 
						Command="{Binding Path=DeleteCommand, ElementName=uc}" 
						CommandParameter="{Binding Path=TagIdStr, ElementName=uc}"
						MouseAction="LeftClick" />
                </Image.InputBindings>
            </Image>
            <Border 
				Width="16" 
				Height="16"
				Background="{Binding Path=ColorStr, ElementName=uc}"
				CornerRadius="100">
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
